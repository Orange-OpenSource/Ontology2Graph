# Model Folder

This folder contains a text file that references several Large Language Models (LLMs). The file can be used as is or modified according to the specific API or service you use to call an LLM.

## Usage
- Use the provided text file to reference available LLMs for your project.
- Modify the file as needed to match the requirements or endpoints of the API you are using to interact with LLMs.

## Example
- The existing text file lists example LLMs and can serve as a template for your own configuration.

Feel free to update or extend the file to suit your needs.