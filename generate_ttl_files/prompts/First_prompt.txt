{Given a turtle schema (Noria.ttl) describing classes and relations, provide a turtle result file that describes a new synthetic knowledge graph based on this specific schema.You must : 

1) Create synthetic knowledge graph turtle file in respect with RDF, RDFS, OWL vocabularies and TURTLE syntax by following all each follwoing steps.
2) The synthtetic knowledge graph turtle file must as big as possible, but lower to 65 000 output token to avoid truncated statement, with a maximum number of entities.
3) Create entities based on all the owl class declared in the provided schema. 
4) link each node to at least one another node with an ObjectProperties. 
5) Use all the ObjectProperties declared in the provided schema to link the entities together. 
6) Group entities declaration and their relationships in the same statement and Use rdfs:label and rdfs:comment to provide more clarity on Entities.
7) Create literals on each entites created based on the DatatypeProperty declared in the provided shcema.
8) The last entitie statement of the synthetic knowledge graph turtle file musn't be truncated
9) Never start a statement with punctuation mark.
10) Never use ldp prefix.
11) Be careffull of syntax related to XMLSchema and xsd prefix each value must be surrounded by quote like that : "value"^^xsd:type, if it is not the case, correct it. 
12) Declare each prefix at the begining of the synthetic knowledge graph turtle result file with @prefix.
13) Never put explanantions at the end of the synthetic knowledge graph turtle file

Be creative
}
